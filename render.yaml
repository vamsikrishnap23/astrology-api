services:
  - type: web
    name: vedic-backend
    env: python
    region: oregon
    plan: starter
    pythonVersion: 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port ${PORT}
